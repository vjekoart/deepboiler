export interface ModelDummy {
  id: number
  uid: string
  title: string
}

export interface ModelUser {
  id: number
  authorized: boolean
}

export interface ModelItem {
  id: number
  name: string
}
